#include <stdio.h>

/**
 * add - This function adds any input given to it
 * @a: The first number to add
 * @b: The second number to add
 * Return: The sum
 */
int add(int a, int b)
{
	return (a + b);
}
